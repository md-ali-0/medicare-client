import { Helmet } from "react-helmet-async";

const Settings = () => {
    return (
        <div>
            <Helmet>
                <title>Dashboard | Settings</title>
            </Helmet>
        </div>
    );
};

export default Settings;
